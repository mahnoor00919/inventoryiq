# Local development with PostgreSQL
# Use a local Postgres instance or a managed provider.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/inventory_db?schema=public"

# For production on Vercel, use a hosted Postgres URL instead.
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/inventory_db?schema=public"

# CHANGE THIS in production — must be at least 32 random characters
JWT_SECRET="change-me-to-a-long-random-secret-minimum-32-chars"

NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"
SMTP_USER="user@gmail.com"
SMTP_PASS="hojfrqdwmnhppesw"
SMTP_FROM_NAME="inventoryiq"
SMTP_FROM_EMAIL="user@gmail.com"
OPENROUTER_API_KEY="your-openrouter-api-key"
